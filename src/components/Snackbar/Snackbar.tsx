import React, { ReactElement, useEffect } from "react";

interface ISnackbarProps {

}
const Snackbar: React.FC<ISnackbarProps> = (props):ReactElement => {

  
  useEffect(() => {

  return () => {};
  }, []);

  return (
       <>
       </>
    )

};

  export default Snackbar;