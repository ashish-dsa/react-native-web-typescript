import * as React from "react";
import { {{ properCase name }} as Paper{{ properCase name }},  {{ properCase name }}Props }from "react-native-paper";

const {{ properCase name }}: React.FC<{{ properCase name }}Props> = (props) => (
  <Paper{{ properCase name }} {...props}>
    {props.children}
  </Paper{{ properCase name }}>
);

export default {{ properCase name }};
